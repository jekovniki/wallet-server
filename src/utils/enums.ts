export enum TransactionType {
    WITHDRAW = 0,
    DEPOSIT = 1
}

export enum Transactions {
    DEFAULT = 10
}

export enum Roles {
    BASIC = 1,
    INTERMEDIATE = 2,
    ADVANCED = 3,
    FULL = 4
}